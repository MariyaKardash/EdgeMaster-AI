import { withAlphaHex } from '@/theme/color-utils';

export const STAT_LABELS = ['STR', 'DEX', 'INT'] as const;

export const CARD_BORDER_DEFAULT = withAlphaHex('primary', 0.1);
export const CHECK_BORDER_DEFAULT = withAlphaHex('outlineVariant', 0.3);
