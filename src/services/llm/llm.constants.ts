/** Default context window for chat, new chapter, and event flows. */
export const LLM_DEFAULT_CTX_SIZE = 1024;

/** Loaded once for all features — matches chapter summarize requirements. */
export const LLM_MAX_CTX_SIZE = 2048;
